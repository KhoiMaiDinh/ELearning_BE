export const UsernameRegex = {
  regex: /^[a-zA-Z0-9]+([._]?[a-zA-Z0-9]+)*$/,
  message:
    'The username is invalid. It must start with an alphanumeric character and can contain dots or underscores, but not consecutively.',
};
